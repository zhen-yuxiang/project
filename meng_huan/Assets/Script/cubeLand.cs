using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cubeLand : MonoBehaviour
{
    public int CubeX;
    public int CubeY;
    public void SetSubeLandInfo (int _x, int _y)
    {
        CubeX = _x;
        CubeY = _y;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
